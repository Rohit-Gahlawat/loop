/**
 * Writes a vector for each feedback item so semantic retrieval can find it.
 * Called once per item on ingest.
 */
export async function embedBatch(
  feedbackIds: string[],
  workspaceId: string,
): Promise<void> {
  void feedbackIds;
  void workspaceId;
}
